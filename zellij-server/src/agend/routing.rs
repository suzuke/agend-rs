//! Routing engine — dispatches messages between channels and agent instances.

/// Placeholder for the routing engine.
pub struct Router;

impl Router {
    pub fn new() -> Self {
        Self
    }
}
