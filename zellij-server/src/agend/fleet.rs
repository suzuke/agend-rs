//! Fleet manager — orchestrates agent instances across panes/tabs.

/// Placeholder for the fleet manager.
pub struct FleetManager;

impl FleetManager {
    pub fn new() -> Self {
        Self
    }
}
