//! Configuration — reads and validates fleet.yaml.

/// Placeholder for AgEnD configuration.
pub struct AgendConfig;

impl AgendConfig {
    pub fn load() -> Self {
        // TODO: read fleet.yaml, merge defaults, load .env
        Self
    }
}
